{{ "{% extends 'ERPGameBundle::layout.html.twig' %}" }}

{{ '{% block body %}' }}
<div class="page-header">
    <h1>Modifier un {{ entity|lower }}</h1>
</div>


<form action="{{ "{{ path('"~ route_name_prefix ~"_update', { 'id': entity.id }) }}" }}" method="post" {{ "{{ form_enctype(edit_form) }}" }} novalidate="novalidate">
    {{ "{{ form_widget(edit_form) }}" }}
    <p>
        <button class="btn btn-primary btn-large" type="submit">Enregistrer</button>
    </p>
</form>

{% set hide_edit, hide_delete = true, false %}
{% include 'views/others/record_actions.html.twig' %}
{{ '{% endblock body %}' }}