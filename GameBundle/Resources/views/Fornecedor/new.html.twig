{% extends 'ERPGameBundle::layout.html.twig' %}

{% block body %}
<div class="page-header">
    <h1>Ajouter un fornecedor</h1>
</div>

<form action="{{ path('fornecedor_create') }}" method="post" {{ form_enctype(form) }} novalidate="novalidate">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    </p>
</form>
	
</div>{% endblock body %}