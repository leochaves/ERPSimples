{% extends 'ERPGameBundle::layout.html.twig' %}

{% block body -%}
    <div class="page-header">
        <h1>Cadastro: Game</h1>
    </div>

    <form action="{{ path('game_create') }}"  class="form-horizontal" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form,{'form_type': 'horizontal'}) }}
        
	    <div class="control-group">
	    	<div class="controls">
	      		 <button type="submit" class="btn btn-primary">Criar</button>
	    	</div>
            </div>
    </form>

<div class="form-actions">
    <a href="{{ path('fornecedor') }}" class="btn">
            Voltar para Lista
    </a>
</div>         
{% endblock %}
